networks:
    lernsax:
      name: lernsax
      external: true

services:
  app:
    build: .
    container_name: lernsax_history
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./files:/files:ro
      - lernsax_git:/git
    networks:
      - lernsax

volumes:
  lernsax_git:
    name: lernsax_git
    external: true